extends layout

block content
  h1= title
  p Welcome to #{title}
  <a href="/new">New Message</a>

  ul
      each message in messages
          li= message.user
          p #{message.text}
          p #{message.createdAt}
  
